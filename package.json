{
  "name": "react-redux-template",
  "version": "1.0.0",
  "description": "A template for react-redux projects",
  "main": "index.js",
  "dependencies": {
    "axios": "^0.21.1",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "helmet": "^4.4.1",
    "salesforce-marketing-cloud-sdk": "^1.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server",
    "build": "cd react-ui/ && npm install && npm run build"
  },
  "author": "",
  "license": "ISC"
}
